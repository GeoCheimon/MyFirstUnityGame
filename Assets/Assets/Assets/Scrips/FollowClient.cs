using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FollowClient : MonoBehaviour
{
    public GameObject Ch03_nonPBR;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //transform.position = Ch03_nonPBR.transform.position;
        //transform.position = Ch03_nonPBR.transform.position + new Vector3(0, 2, -3);
    }
}
